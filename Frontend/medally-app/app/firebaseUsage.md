// Usage examples for firebaseHelpers.js
// Import these functions in your components as needed

import { register, login, logout, saveUserData, getUserData, updateUserData } from './firebaseHelpers';

// Register a new user
// await register('email@example.com', 'password123');

// Login
// await login('email@example.com', 'password123');

// Logout
// await logout();

// Save user data
// await saveUserData(uid, { name: 'John Doe', age: 30 });

// Get user data
// const data = await getUserData(uid);

// Update user data
// await updateUserData(uid, { age: 31 });
